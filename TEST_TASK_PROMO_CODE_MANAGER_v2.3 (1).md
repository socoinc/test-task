# Тестовое задание: Мини-приложение "PromoCode Manager"

**Длительность:** 8-10 часов
**Результат:** Публичный Git-репозиторий с готовым проектом

---

## Цель

Создать fullstack приложение для управления промокодами с аналитикой.

Задание проверяет понимание **CQRS-подхода (Command Query Responsibility Segregation)** — архитектуры, в которой пути записи и чтения данных разделены между разными хранилищами:

- **Command (запись):** мутации данных идут в основную БД (MongoDB)
- **Query (чтение для таблиц):** все таблицы на фронтенде читают данные из аналитической БД (ClickHouse)
- **Синхронизация:** при каждой мутации в MongoDB данные реплицируются в ClickHouse

---

## Технический стек

| Слой           | Технологии                                                                             |
| -------------- | -------------------------------------------------------------------------------------- |
| Backend        | NestJS, TypeScript, Mongoose, @clickhouse/client                                       |
| Frontend       | React (или Preact), TypeScript, любая UI-библиотека — SPA, без SSR (не Next.js)       |
| Базы данных    | MongoDB, ClickHouse, Redis                                                             |
| Инфраструктура | Docker Compose                                                                         |

### Frontend: выбор библиотек

Выбор UI-библиотеки и компонентов таблиц — на ваше усмотрение. Примеры популярных решений:

**UI-фреймворки:** MUI (Material UI), Ant Design, Mantine, Chakra UI, shadcn/ui, Tailwind CSS

**Таблицы с server-side поддержкой:** material-react-table, TanStack Table, AG Grid, Ant Design Table

**Формы и валидация:** React Hook Form + Zod/Yup, Formik

**State management / data fetching:** TanStack Query (React Query), SWR, Zustand, Redux Toolkit

Главное — чтобы таблицы поддерживали **server-side пагинацию, сортировку и фильтрацию**, а не загружали все данные в память.

---

## Бизнес-требования

### Сущности

**Пользователи:**
- Базовая информация (email, имя, телефон)
- Пароль (хранится в хэшированном виде)
- Статус активности

**Промокоды:**
- Уникальный код (например, "SUMMER2024")
- Процент скидки
- Лимиты использования (общий и на пользователя)
- Срок действия (опционально: дата начала и окончания)
- Статус активности

**Заказы (упрощённые):**
- Принадлежность пользователю (для агрегации статистики)
- Сумма заказа
- Применённый промокод (опционально)
- Дата создания
- Реализация может быть моковой — главное, чтобы были данные для аналитики

**Использования промокодов:**
- Какой промокод, кем и к какому заказу применён
- Рассчитанная сумма скидки
- Дата использования

### Функциональность

**Авторизация и регистрация:**
- Регистрация нового пользователя (email, пароль, имя, телефон)
- Вход по email и паролю
- JWT токены для аутентификации запросов
- Защита API endpoints (только авторизованные пользователи)

**CRUD операции:**
- Создание, чтение, обновление, деактивация пользователей и промокодов

**Заказы:**
- Создание заказа (сумма, принадлежность пользователю). Заказ существует **независимо** от промокода
- Просмотр своих заказов

**Применение промокода:**
- Отдельный endpoint для применения промокода к **уже существующему** заказу
- Это два разных действия: сначала пользователь создаёт заказ (`POST /orders`), затем применяет промокод к этому заказу (`POST /orders/:id/apply-promocode`)
- Валидация: существование заказа, существование и активность промокода, лимиты, срок действия, промокод ещё не применён к этому заказу
- Расчёт и сохранение скидки

**Таблицы на фронтенде (данные из ClickHouse):**
- Таблица пользователей с агрегированной статистикой (заказы, суммы, использованные промокоды)
- Таблица промокодов с метриками эффективности (использования, выручка, уникальные пользователи)
- История использований промокодов

**Требования к таблицам:**
- Server-side пагинация, сортировка, фильтрация по колонкам
- Глобальный фильтр по диапазону дат (важно для производительности ClickHouse)
- Пресеты дат: "Сегодня", "Последние 7 дней", "Последние 30 дней", "Произвольный"
- Загрузка всех записей в память на клиенте (`findAll()` без лимита) — **недопустима**

---

## Архитектура данных (CQRS)

Это **ключевой аспект задания**. Приложение должно реализовать разделение ответственности между базами данных:

- **MongoDB** — источник истины, CRUD операции, валидация бизнес-правил
- **ClickHouse** — аналитика, агрегаты, **все таблицы на фронтенде читают данные отсюда**
- **Redis** — кэширование, сессии, блокировки

### Роль Redis

Redis — **обязательный** компонент архитектуры. Примеры использования (реализуйте хотя бы два):

- **Кэширование** аналитических запросов к ClickHouse (результаты таблиц с коротким TTL)
- **Distributed lock** при применении промокода — предотвращение race condition, когда два запроса одновременно применяют один промокод и превышают лимит
- **Хранение сессий** или refresh-токенов
- **Rate limiting** на критичных endpoint'ах (login, apply-promocode)

Redis должен присутствовать в Docker Compose и использоваться в коде бэкенда. Просто добавить Redis в docker-compose без реального использования — недостаточно.

### Таблицы в ClickHouse

Все сущности из MongoDB должны быть **продублированы** в ClickHouse в денормализованном виде. Минимальный набор таблиц:

| Таблица ClickHouse | Содержимое                                                              |
| ------------------- | ----------------------------------------------------------------------- |
| `users`             | Данные пользователей (имя, email, телефон, дата создания, статус и пр.) |
| `promocodes`        | Данные промокодов (код, скидка, лимиты, даты, статус и пр.)             |
| `orders`            | Заказы (сумма, промокод, пользователь, дата и пр.)                      |
| `promo_usages`      | История использований промокодов                                        |

Данные в ClickHouse должны быть **самодостаточными** — аналитические запросы не должны обращаться к MongoDB. Например, таблица `promo_usages` должна содержать не только `userId`, но и имя/email пользователя; не только `promocodeId`, но и код промокода.

### Синхронизация MongoDB → ClickHouse

При каждой мутации в MongoDB (создание, обновление, деактивация) данные должны реплицироваться в ClickHouse. Конкретный механизм синхронизации — решение за вами (sync-on-write, event-driven, periodic batch и т.д.).

### Примеры data flow

**Пример 1: Создание промокода**
1. `POST /promocodes` → валидация DTO → запись в MongoDB
2. После записи → синхронизация в таблицу `promocodes` ClickHouse
3. Таблица промокодов на фронте → `GET /promocodes?page=1&pageSize=10&sortBy=createdAt` → запрос к ClickHouse

**Пример 2: Создание заказа и применение промокода (два отдельных действия)**
1. `POST /orders` → создание заказа (сумма, пользователь) → запись в MongoDB → синхронизация в ClickHouse
2. `POST /orders/:id/apply-promocode` → валидация промокода → обновление заказа (скидка) + запись использования в MongoDB → синхронизация `orders` и `promo_usages` в ClickHouse
3. Аналитическая таблица → агрегирующий запрос к ClickHouse (GROUP BY, SUM и пр.)

**Пример 3: Таблица пользователей с аналитикой**
1. Фронтенд запрашивает `GET /analytics/users?page=1&pageSize=10&sortBy=totalSpent&dateFrom=...`
2. Бэкенд выполняет запрос к ClickHouse с JOIN/GROUP BY
3. Ответ содержит готовые данные с именами, email, метриками — **без дополнительных запросов к MongoDB**

### Типичные ошибки (антипаттерны)

- Хранить в ClickHouse только ID без человекочитаемых данных (имя, email, код промокода) — аналитика будет показывать ObjectId вместо имён
- Иметь единственную таблицу в ClickHouse (например, только `promo_usages`) — недостаточно для полноценной аналитики
- Строить аналитику через запросы к MongoDB, а ClickHouse использовать "для галочки"
- Загружать все записи через `findAll()` и делать пагинацию/сортировку на клиенте
- Совмещать создание заказа и применение промокода в одном endpoint — это разные бизнес-операции

---

## Критерии оценки

Оценка производится по чеклисту. Каждый пункт оценивается по трёхуровневой шкале:

- **0** — не реализовано или не работает
- **1** — удовлетворительно (базовая реализация, работает)
- **2** — отлично (продуманная реализация, edge-cases учтены)

Это позволяет двум разным проверяющим прийти к одинаковому результату, при этом различая "сделано кое-как" и "сделано хорошо".

> **Примечание:** Модули Users и Orders оцениваются только на базовую работоспособность. Основной фокус — промокоды и аналитика.

---

### Запуск и работоспособность (макс. 6)

| # | Критерий | 1 — Удовлетворительно | 2 — Отлично | Балл |
|---|----------|-----------------------|-------------|------|
| 1 | Docker Compose | Инфраструктура поднимается, но требуются ручные действия (env-файлы, порядок запуска) | `docker-compose up` — и всё работает из коробки, volumes, healthchecks | |
| 2 | Backend | Запускается с предупреждениями или после ручных шагов (npm install, seed) | Запускается чисто, API отвечает, README содержит инструкцию | |
| 3 | Frontend | Открывается, но есть ошибки в консоли или неработающие страницы | Работает без ошибок, можно зарегистрироваться и войти | |

---

### CQRS и ClickHouse (макс. 16)

| # | Критерий | 1 — Удовлетворительно | 2 — Отлично | Балл |
|---|----------|-----------------------|-------------|------|
| 4 | CRUD → MongoDB | Запись идёт в MongoDB, но часть операций обходит валидацию или схему | Все мутации через Mongoose с валидацией, схемы полные | |
| 5 | Таблицы → ClickHouse | Хотя бы одна таблица читает из ClickHouse | Все аналитические таблицы читают из ClickHouse, MongoDB не используется для чтения таблиц | |
| 6 | Синхронизация MongoDB → CH | Есть базовый механизм (sync-on-write), но без обработки обновлений или удалений | Покрыты create + update + deactivate, механизм надёжный (events, batch, или sync-on-write с retry) | |
| 7 | Количество таблиц CH | 3 таблицы | 4 таблицы (users, promocodes, orders, promo_usages) | |
| 8 | Денормализация данных | Частичная — есть некоторые имена/email, но не везде | Полная — все таблицы самодостаточны, аналитика показывает человекочитаемые данные | |
| 9 | Автономность запросов | Аналитика работает из CH, но есть единичные fallback-запросы к MongoDB | Запросы полностью автономны, MongoDB не вызывается при чтении аналитики | |
| 10 | Автосоздание таблиц CH | Таблицы создаются скриптом, но нужно запускать вручную | Таблицы создаются автоматически при старте приложения (init-сервис или миграция) | |
| 11 | Обработка ошибок синхронизации | try-catch с логированием ошибки | try-catch + retry-логика или dead letter queue, ошибка не блокирует основную операцию | |

---

### Backend (макс. 20)

| # | Критерий | 1 — Удовлетворительно | 2 — Отлично | Балл |
|---|----------|-----------------------|-------------|------|
| 12 | `POST /orders` | Создание заказа работает, но без валидации суммы или пользователя | Создание с валидацией, корректный DTO, заказ привязан к текущему пользователю | |
| 13 | `POST /orders/:id/apply-promocode` | Отдельный endpoint существует и работает | Endpoint с полной валидацией: заказ принадлежит пользователю, промокод не применён ранее, корректные HTTP-коды | |
| 14 | Валидация промокода | Проверяется хотя бы активность и один из лимитов | Проверяются все условия: активность, общий лимит, лимит на пользователя, срок действия (dateFrom/dateTo) | |
| 15 | Защита от повторного применения | Промокод нельзя применить дважды к одному заказу | + нельзя применить второй промокод к заказу, где уже есть промокод | |
| 16 | DTO и валидация входных данных | DTO есть, базовые декораторы class-validator (@IsString, @IsNumber) | Полная валидация: @IsEmail, @MinLength, @IsPositive, вложенные DTO, transform-декораторы | |
| 17 | JWT аутентификация | Токен проверяется, endpoints защищены | Access + refresh токены, корректный expire, guard на уровне контроллера или глобально | |
| 18 | Скрытие passwordHash | Пароль не виден в основных endpoint'ах | `select: false` в схеме или Exclude в DTO, проверено на всех endpoint'ах (включая вложенные объекты) | |
| 19 | Параметризация запросов CH | Запросы параметризованы в большинстве случаев | Все запросы параметризованы, нигде нет строковой интерполяции пользовательского ввода | |
| 20 | Redis (реальное использование) | 1 реальный use-case (например, только кэш или только сессии) | ≥ 2 use-case (lock + кэш, rate-limit + сессии и т.д.) | |
| 21 | Обработка ошибок API | Возвращаются корректные HTTP-коды (400, 404) | Коды + понятные сообщения об ошибках, кастомные exception filters или стандартизированный формат | |

---

### Frontend (макс. 16)

| # | Критерий                        | 1 — Удовлетворительно                                                                              | 2 — Отлично                                                                                      | Балл |
|---|---------------------------------|----------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------|------|
| 22 | Аналитические таблицы           | 2 таблицы                                                                                          | ≥ 3 таблицы: пользователи с аналитикой, промокоды с метриками, история использований             | |
| 23 | Server-side пагинация           | Пагинация работает, данные подгружаются постранично                                                | + отображение общего количества, выбор pageSize, loading-состояние                               | |
| 24 | Server-side сортировка          | Сортировка по 1-2 колонкам                                                                         | Сортировка по всем значимым колонкам, визуальная индикация направления                           | |
| 25 | Глобальный фильтр по датам      | DatePicker с произвольным выбором диапазона                                                        | + пресеты (Сегодня / 7 дней / 30 дней), фильтр применяется ко всем таблицам                      | |
| 26 | Валидация форм                  | Обязательные поля отмечены, базовая проверка                                                       | Валидация форматов (email, мин. длина пароля), inline-ошибки, блокировка submit до исправления   | |
| 27 | Управление заказами/промокодами | Частично: создание промокодов или заказов                                                          | Создание/редактирование промокодов, создание заказов, просмотр своих заказов, применение промокода к заказу | |
| 28 | Отображение ошибок API          | alert() или console + визуальное уведомление                                                       | Toast/snackbar с осмысленным текстом ошибки, разные стили для разных типов (error/success/warning) | |
| 29 | Защита роутов                   | Редирект на логин при отсутствии токена                                                            | + проверка expire токена, автоматический logout, сохранение целевого URL после логина            | |

---

### TypeScript (макс. 8)

| # | Критерий | 1 — Удовлетворительно | 2 — Отлично | Балл |
|---|----------|-----------------------|-------------|------|
| 30 | Отсутствие `any` | Минимум `any` (1-3 случая), не в критичных местах | Ноль `any` в бизнес-логике, `unknown` для catch, дженерики где уместно | |
| 31 | DTO / интерфейсы | Описаны для основных endpoint'ов | Полное покрытие: входные DTO, выходные DTO/интерфейсы, query-параметры, ClickHouse-ответы | |
| 32 | Strict mode | `strictNullChecks: true` или `strict: true` включён | + нет `@ts-ignore` / `@ts-expect-error`, код компилируется без ошибок в strict mode | |
| 33 | Типы ClickHouse | Базовые интерфейсы для CH-ответов | Типизированные row-маппинги, generic-обёртки для CH-клиента, нет `as any` при парсинге | |

---

### Бонусные баллы (макс. 12)

| # | Критерий | 1 — Удовлетворительно | 2 — Отлично | Балл |
|---|----------|-----------------------|-------------|------|
| 34 | Swagger / OpenAPI | Базовые аннотации (@ApiTags, @ApiResponse) | Полная документация: описания, примеры запросов/ответов, схемы DTO, авторизация | |
| 35 | Unit-тесты | 3-5 тестов на бизнес-логику | ≥ 8 тестов, покрыты edge-cases (expired промокод, лимит = 0, невалидный ввод), моки сервисов | |
| 36 | Race condition | Базовая защита (findOneAndUpdate или простой Redis lock) | Distributed lock с TTL + обработка таймаута, тест на параллельное применение | |
| 37 | README | Инструкция запуска | + описание архитектуры: таблицы CH, механизм синхронизации, server-side подход, диаграммы | |
| 38 | Кэш-менеджмент | Инвалидация кэша после мутаций (refetch) | Оптимистичный UI + инвалидация, loading/stale состояния, TanStack Query или аналог | |
| 39 | Фильтрация по колонкам | Фильтр по 1-2 колонкам (текстовый поиск) | Фильтры по нескольким колонкам, разные типы (текст, число, enum/select), server-side обработка | |

---

### Шкала оценки

| Баллы | Уровень |
|-------|---------|
| **62-78** | Отлично |
| **48-61** | Хорошо |
| **33-47** | Удовлетворительно |
| **< 33** | Недостаточно |

**Максимум:** 66 основных + 12 бонусных = **78 баллов**

---

## Что предоставить

1. Ссылка на публичный GitHub репозиторий
2. README с инструкцией запуска (Docker Compose для БД, команды для backend/frontend)
3. Краткое описание принятых архитектурных решений (в README или отдельном файле):
   - Какие таблицы созданы в ClickHouse и почему
   - Как организована синхронизация MongoDB → ClickHouse
   - Как реализованы server-side операции в таблицах

---

## Контакты

При возникновении вопросов — обращайтесь к рекрутеру или техническому специалисту, который выдал задание.

Удачи!